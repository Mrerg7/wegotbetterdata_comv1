# Host-based redirects (www→apex, HTTP→HTTPS) are handled by src/worker.js.
# Absolute URLs in this file cause Workers error 100324 — use relative paths only.

/search    /    301
/search/*  /    301
