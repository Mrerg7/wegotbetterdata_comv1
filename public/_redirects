# Host-based redirects (www→apex, HTTP→HTTPS) are configured as Cloudflare Redirect Rules at the zone level.
# _redirects under Workers Static Assets only supports relative paths (error 100324).
# See: Cloudflare Dashboard → wegotbetterdata.com → Rules → Redirect Rules
